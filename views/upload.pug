extends layout

block content
  h1 女優DBデータ更新用ファイルのアップロード
  form(action="/upload" method="POST" enctype="multipart/form-data")
    input(type="file" name="file")
    button(type="submit") 送信
  h2 #{status}